<div>
                <div>
                    <h5 class="modal-title" id="exampleModalLongTitle">Modal title</h5>
                    <input type="text" aria-label="First name" className="form-control w-50  " placeholder="Titel Name"

                        onChange={this.handelchage}
                    />
                    <input type="text" aria-label="First name" className="form-control w-50  "
                        onChange={this.handelchage} placeholder=" img" />
                    <input type="text" aria-label="First name" className="form-control w-50  "
                        onChange={this.handelchage} placeholder=" rates" />
                    <button type="button" class="btn btn-primary"

                    >Save changes</button>
                </div>
            </div>



            <div className="container d-flex justify-content-around">

                {filteredMovies.length === 0 ?
                    (itemmovies.map((el, i) => (
                        <div className="row w-50 ">
                            <div className="row shadow p-3 mb-5 bg-white rounded">
                                <Movies itemmovies={el} key={i} />

                            </div>
                        </div>
                    ))) :
                    filteredMovies.map((el, i) => (
                        <div className="row w-50 ">
                            <div className="row shadow p-3 mb-5 bg-white rounded">
                                <Movies itemmovies={el} key={i} />

                            </div>
                        </div>
                    ))
                }



            </div>